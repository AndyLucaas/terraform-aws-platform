apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "backend.name" . }}
  labels:
    {{- include "backend.labels" . | nindent 4 }}
spec:
    {{- with .Values.config }}
    data:
        {{- toYaml . | nindent 4 }}
    {{- end }}