apiVersion: v1
kind: ConfigMap
metadata:
  name: backend-config
  namespace: dev
data:
  DB_URL: jdbc:postgresql://postgres-service:5432/itdesk
  SERVER_PORT: "8080"
  CORS_ALLOWED_ORIGINS: "http://app.itticket.local,http://localhost:30080,http://localhost:5173"
  ATTACHMENTS_PATH: /var/itdesk/attachments