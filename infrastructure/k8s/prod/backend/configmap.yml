apiVersion: v1
kind: ConfigMap
metadata:
  name: backend-config
  namespace: prod
data:
  DB_URL: jdbc:postgresql://postgres-service.prod.svc.cluster.local:5432/itdesk_prod
  SERVER_PORT: "8080"
  CORS_ALLOWED_ORIGINS: "https://app.itticket.local"
  ATTACHMENTS_PATH: /var/itdesk/attachments
