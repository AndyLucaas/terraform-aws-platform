apiVersion: v1
kind: Secret
metadata:
  name: backend-secret
  namespace: itdesk
type: Opaque
stringData:
  # Clé de signature HMAC-SHA256 des JWT — 32 caractères minimum.
  # Générez-la avec : openssl rand -base64 48
  # La changer déconnecte immédiatement tous les utilisateurs.
  JWT_SECRET: "/l30UtJiK1QBbbF0Gke390ZS32XgtshvcO34TftZuTrklRuhpbS3a4pN+hrgJCPf"
  JWT_ACCESS_TOKEN_VALIDITY_SECONDS: "900"
  JWT_REFRESH_TOKEN_VALIDITY_SECONDS: "604800"