apiVersion: v1
kind: ConfigMap
metadata:
  name: backend-config
  namespace: staging
data:
  DB_URL: jdbc:postgresql://postgres-service.staging.svc.cluster.local:5432/itdesk_staging
  SERVER_PORT: "8080"
  CORS_ALLOWED_ORIGINS: "http://app-staging.itticket.local,https://app-staging.itticket.local"
  ATTACHMENTS_PATH: /var/itdesk/attachments
