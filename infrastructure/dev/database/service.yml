apiVersion: v1
kind: Service
metadata:
  name: postgres-service
  namespace: dev
spec:
  selector:
    app: database
  type: ClusterIP
  ports:
  - name: db-port
    protocol: TCP
    port: 5432
    targetPort: 5432
